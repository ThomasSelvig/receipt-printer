version: '3.8'

services:
  discord-bot:
    build: .
    privileged: true
    devices:
      - /dev/bus/usb/001/003:/dev/bus/usb/001/003
    volumes:
      - /dev:/dev
      - /run/udev:/run/udev:ro
    env_file:
      - .env
    restart: unless-stopped
{
  "config": {
    "step": {
      "user": {
        "title": "Receipt Printer",
        "description": "Enter the host and port where api.py is running (e.g. 192.168.1.100 and 8000).",
        "data": {
          "host": "Host",
          "port": "Port"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the printer service at that host/port."
    },
    "abort": {
      "already_configured": "That printer is already configured."
    }
  }
}